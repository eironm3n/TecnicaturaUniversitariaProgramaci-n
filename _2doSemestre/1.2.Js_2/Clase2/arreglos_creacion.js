// esta es una forma antigua de hacer un arreglo, ya no se usa, pero existió

// let autos = new Array('Ferrari', 'Renault', 'BMW');

const autos = ['Ferrari', 'Renault', 'BMW'] // esta es la forma actual de arreglo
console.log(autos);
